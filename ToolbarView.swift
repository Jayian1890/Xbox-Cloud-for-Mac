//
//  ToolbarView.swift
//  Xbox Cloud
//
//  Created by Jared T on 2/25/23.
//
/*
import SwiftUI

struct MyToolbar: ToolbarContent {
    
    var body: some ToolbarContent {
        
        ToolbarItem(placement: .primaryAction) {
            Button(action: ContentView.view!.recordVideo) {
                Label("Record", systemImage: "video.circle")
                    .labelStyle(.titleAndIcon)
                    .foregroundColor(ContentView.view!.videoButtonColor)
            }.keyboardShortcut("r", modifiers: .command)
        }
        
        ToolbarItem(placement: .primaryAction) {
            Button(action: Screenshot().takeSnapshot) {
                Label("Screenshot", systemImage: "photo.circle")
                    .labelStyle(.titleAndIcon)
            }.keyboardShortcut(KeyEquivalent("s"), modifiers: .command)
        }
        
    }
}*/
